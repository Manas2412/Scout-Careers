"""The company and source registry.

What the operator actually curates: which employers are worth watching, at what
tier, and which board each one publishes to. ``COMPANY_REGISTRY.md`` is the
specification; :mod:`~scout_careers.registry.service` is the whole of Phase 1's
implementation of it.
"""

from __future__ import annotations
