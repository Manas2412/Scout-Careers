"""Cross-cutting primitives: config, types, errors, logging, time, hashing, IDs.

`common/` imports nothing else from the package. That rule is enforced by an
import-linter contract in the pre-merge gate (DEVELOPMENT.md §2.1).
"""

from __future__ import annotations
